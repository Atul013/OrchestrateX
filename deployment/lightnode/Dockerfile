# Use a minimal base image to avoid the 'No source image provided with FROM' error
FROM busybox:1.36

# Set working directory (optional)
WORKDIR /app

# Default command
CMD ["sh","-c","echo Container is running"]
